export interface SavedSearch {
  id: string;
  resourceLink: string;
  createDate: string;
}

export interface wizardMetadata {
  id: string;
  link: string;
  createDate: string;
  title: string;
  project: string;
  name: string;
  email: string;
  description: string;
  comments: string;
}
